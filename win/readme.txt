Windows only:

The tinyxml lib must be added manually. To do so, please unpack tinyxml_2_6_2.zip to win/tinyxml/. 
The library is required to compile dvdetect.dll. The files are incorporated directly, so you do not 
need to build it separately.